# Base URL of the deployed backend API, including /api.
# Leave unset for local dev — vite.config.js proxies /api to localhost:5000.
# In production (Netlify/Vercel), set this to your backend's URL, e.g.
# VITE_API_URL=https://document-summary-backend.onrender.com/api
VITE_API_URL=
